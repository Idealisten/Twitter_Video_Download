services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: twitter-video-download-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - twitter-video-download
